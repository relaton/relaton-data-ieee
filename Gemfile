source 'https://rubygems.org'

# The relaton monorepo supplies both the IEEE flavor (Relaton::Ieee::DataFetcher,
# INDEXFILE = "index-v2") and the vendored index engine (Relaton::Index).
gem 'relaton', git: 'https://github.com/relaton/relaton.git', branch: 'main'
gem 'pubid',   git: 'https://github.com/metanorma/pubid.git',  branch: 'main'
